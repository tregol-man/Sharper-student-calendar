<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Calendar.JoinCreatePage"
             Title="JoinCreatePage">
    <VerticalStackLayout>
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Border BackgroundColor="White" VerticalOptions="Center">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="15"/>
            </Border.StrokeShape>
            <Button x:Name="CreateGroupButton"
            HeightRequest="50"
            Clicked="OnCreateGroupClick"
            Text="Create Group"
            FontFamily="Inter"
            FontAttributes="Bold"
            TextColor="#4F5171"
            BackgroundColor="Transparent"/>
        </Border>

        <Border BackgroundColor="White" VerticalOptions="Center">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="15"/>
            </Border.StrokeShape>
            <Button x:Name="JoinGroupButton"
            HeightRequest="50"
            Clicked="OnJoinGroupClick"
            Text="Join Group"
            FontFamily="Inter"
            FontAttributes="Bold"
            TextColor="#4F5171"
            BackgroundColor="Transparent"/>
        </Border>
        <Border BackgroundColor="White" VerticalOptions="Center">
            <Button x:Name="LogOutButton"
        HeightRequest="50"
        Clicked="OnLogOutClick"
        Text="Log Out"
        FontFamily="Inter"
        FontAttributes="Bold"
        TextColor="#4F5171"
        BackgroundColor="Transparent"/>
        </Border>
    </VerticalStackLayout>
</ContentPage>